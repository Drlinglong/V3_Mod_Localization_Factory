# Set default behavior, in case users don't have core.autocrlf set.
* text=auto eol=lf

# Explicitly declare text files we want to always be LF
*.py text eol=lf
*.md text eol=lf
*.json text eol=lf
*.txt text eol=lf
*.yml text eol=lf

# Explicitly declare files that will always have CRLF line endings on checkout.
*.bat text eol=crlf